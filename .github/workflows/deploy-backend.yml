name: Deploy Backend

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'data/3.processed.json'
      - 'backend/Dockerfile'
      - 'fly.toml'
      - '.dockerignore'

jobs:
  deploy:
    name: Deploy to Fly.io
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  seed:
    name: Seed Database
    needs: deploy
    if: github.event_name == 'workflow_dispatch' || contains(toJson(github.event.commits.*.modified), 'data/3.processed.json')
    runs-on: ubuntu-latest
    steps:
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl ssh console --app gcg-builder --command "bun db/seed.ts"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
